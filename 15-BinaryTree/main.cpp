#include "BinaryTree.h"


int main() {
    BinaryTree<double> test;

    /*
    test.insert(50);
    test.insert(30);
    test.insert(70);
    test.insert(20);
    test.insert(40);
    test.insert(60);
    test.insert(80);

    std::cout << "Original Tree:\n";
    test.print();

    test.deleteMin();
    std::cout << "\nTree after deleting:\n";
    test.print();
    */

    return 0;
}